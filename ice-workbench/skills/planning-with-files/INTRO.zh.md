# 文件式任务规划（Manus 模式）

用文件做"持久化记忆"来推进复杂多步任务，跨 session 也能恢复。

**触发场景**：步骤超过 5 个 tool 调用的复杂任务、研究型工作、需要边干边记录的项目。
**主要功能**：自动建立 `task_plan.md`（计划）、`findings.md`（发现/中间结果）、`progress.md`（进度），随时可被 `/clear` 后从文件继续。
