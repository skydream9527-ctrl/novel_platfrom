# NL → SQL（自然语言转 SQL）

按结构化对话流程把用户的自然语言需求转成符合规范的 SQL。

**触发场景**：用户说"查指标"、"写 SQL"、"分析数据"、"埋点数据"、"核心指标"，或明确指定"使用 nl-sql"。
**支持业务线**：浏览器主端、浏览器信息流、内容中心、搜索、小说。
**主要功能**：分步确认业务线 → 数据类型（核心指标/埋点）→ 指标 / 维度 / 时间范围；按 nl-sql 三要素模型生成 SQL；输出前自动跑自检（日期格式 YYYYMMDD、字段合法性、表名三段式、分区过滤）。
